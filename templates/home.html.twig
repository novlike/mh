{% extends 'base.html.twig' %}

{% block %}
    {#@todo installer bootstrap ? #}
    {{ form_start(form) }}
    {{ form_row(form.characters) }}
    {{ form_row(form.characters) }}
    {{ form_row(form.characters) }}
    {{ form_row(form.characters) }}
    {{ form_end(form) }}
{% endblock %}